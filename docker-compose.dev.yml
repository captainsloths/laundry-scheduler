version: '3.8'

services:
  laundry-scheduler-dev:
    build: .
    container_name: laundry-scheduler-dev
    ports:
      - "8080:8080"
    volumes:
      - ./static:/app/static:ro
      - ./templates:/app/templates:ro
    environment:
      - TZ=UTC
    restart: unless-stopped
